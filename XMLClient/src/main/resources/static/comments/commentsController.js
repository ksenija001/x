var app = angular.module('comments.controllers', []);

app.controller('commentsController', [
		'$scope',
		'$rootScope',
		'commentsService',
		'$location',
		function($scope, $rootScope, commentsService,
				$location) {
		
		}]);